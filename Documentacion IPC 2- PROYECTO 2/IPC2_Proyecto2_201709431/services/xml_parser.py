import xml.etree.ElementTree as ET
def cargar_configuracion(ruta):
    tree = ET.parse(ruta)
    root = tree.getroot()
    # Parsear drones, plantas, hileras y planes...
    return []
def generar_salida(ruta, datos):
    # Construir árbol XML con datos de simulación
    pass
